<script>
    Vue.component('comment-button', {
        props: ['movie'],
        delimiters: ['${', '}'],
        data: function () {
            return {
                userComments: [
                    {% for comment in app.user.comments %}
                    {{ comment.tmdbId }},
                    {% endfor %}
                ],
            }
        },
        methods: {
            comment: function(movieTMDBId) {
                this.$emit("toggle-modal", this.movie.title);
                console.log("envoie du comment");
            }
        },
        template:
            '<b-button variant="primary" block @click="comment(movie.id)"><b-icon-chat-dots></b-icon-chat-dots></b-button>'
    })
</script>